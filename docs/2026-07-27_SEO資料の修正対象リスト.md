# SEO資料3点の修正対象リスト（2026-07-27）

<context>
docs/ の SEO 資料3点は 2025-05-08 の初回コミット以降、一度も更新されていない。
その間に LLMO（AI検索最適化）フェーズ0〜5 で方針が確定し、サイト上の機能記述も大幅に是正された。
本リストは、現在確定している方針と矛盾する記述 72 行の修正指示である。

行番号は 2026-07-27 時点（コミット d882367）のもの。
修正で行数がずれるため、**必ず下から上へ（行番号の大きい順に）修正すること。**
</context>

<correction>
## ⚠️ 本リストに含まれていた誤り（作業中に判明・訂正済み）

本リストは Super が作成したが、以下3点の誤りがあった。
実装担当が実測で検出し、いずれも手を付けずに報告している。**この訂正が正である。**

| 箇所 | リストの記述 | 実測による訂正 |
|---|---|---|
| 第6部 画像形式の変換 | 「実測で完了済み（画像36件中36件が変換済み）」 | **誤り。** PNG 36件のうち WebP 版があるのは **24件**。未変換12件（OGP画像2件・PWAアイコン10件）。PNG のままである理由は未確認。「すべて変換完了」とは書けない |
| 第2部 柱書き | 「記載8件のうち実在するものがない」 | **誤り。** `/`・`/free-trial`・`/case-studies` の**3件は実在する**。列挙した7行のみが修正対象 |
| 第5部 concrete:180,190 | 「アナリティクスを言い換える」と2箇所を指定 | **190行目は対象外。** 該当語は他社サービス名（Google Analytics）であり、サービス名の改変禁止に抵触する。180行目のみが対象 |

<reflection>
第6部の「修正しないもの（手を付けないこと）」の表に「注記のみ足す」と書いたのも指示の矛盾だった。
「手を付けない」と「注記を足す」は両立しない。実装担当が矛盾を検出して着手を保留し、
別途の指示を受けてから対応した。**リストを書く際、同じ項目に相反する指示を入れないこと。**
</reflection>
</correction>

---

## 第0部【全資料共通】冒頭に注意書きを追加する

3資料それぞれの1行目（H1見出し）の直後に、以下のブロックを挿入する。
資料名の部分だけ各ファイルに合わせて変える。他は同一の文面を使う。

```markdown
> ⚠️ **この資料は 2025年5月時点のものです。**
> その後の AI 検索最適化（LLMO）フェーズ0〜5 で方針が変更された箇所があり、
> 2026-07-27 に矛盾箇所を修正しました。
>
> **最新の確定方針は `docs/handover/` の引き継ぎノート（フェーズ0〜5）を参照してください。**
> 特に以下は本資料より優先します。
> - 「やらないこと（証拠が否定的な施策）」の一覧
> - サービスとして提供している機能・していない機能の確定リスト
> - Cloudflare とクローラー向け設定の変更禁止事項
>
> 本資料に書かれた施策を実施する前に、引き継ぎノートで否定されていないか確認すること。
```

---

## 第1部【最優先】景品表示法リスクを再注入する記述

### 1-1. seo-strategy.md:112 — 存在しないプラン

現状の行は、実在しないプランと実在しない特別機能を、コアページの企画として記載している。
確認済みの事実: 経営者向けのプランは存在せず、そこに挙がっていた4機能も提供していない。
ただし**ページ自体は実在する**（公開URL は `/digital-namecard/for-executives`）。

**指示:**
- 行を削除しない。実在ページの企画として残す
- 「プラン」という語を使わない。ページの性質に合った呼び方に変える
- 「特別機能」の断定をやめる。提供していない機能を想起させる書き方にしない
- URL 欄 `/for-executives/` は実在しないため、実在する `/digital-namecard/for-executives` に直す

---

## 第2部 コアページ表（seo-strategy.md:108〜115）のURLと内容

この表の URL 欄は、記載8件のうち実在するものがない。実在URLに直すか、未作成である旨を書く。

| 行 | 修正内容 |
|---|---|
| 108 | URL を実在パスに直す。実在しなければ「未作成」と明記する |
| 110 | 同上 |
| 111 | 「各プランの詳細比較と特典」→ 機能を比較させる見せ方はすでに廃止済み。人数で選ぶ形に変わっている。「特典」は実在確認が取れていないため書かない |
| 112 | 第1部 1-1 のとおり |
| 113 | 「活用事例集」→「活用シーン集」。実在の事例は1件のみで、事例集は作れない |
| 114 | URL `/faq/` は実在しない。実在は `/support/faq` |
| 115 | URL を実在パスに直す。実在しなければ「未作成」と明記する |

---

## 第3部 種類ごとの一括修正

同じ種類の修正をまとめている。種類ごとに同じ考え方で直すこと。

### 3-1. 被リンク獲得 → ブランド言及の獲得（9行）

対象: seo-strategy.md の 196 / 197 / 198 / 200 / 246 / 254、seo-measure.md の 71 / 73 / 74

確定方針: 被リンクの量的獲得は相関が弱い（0.10〜0.218）。
一方でブランド言及の獲得は最強の相関（0.664）であり、未着手の重要課題として残っている。

**指示:**
- 節や施策を削除しない。ブランド言及を獲得する施策の置き場所として書き換える
- seo-strategy.md:254 の計測指標「被リンク数の増加」は「ブランド言及数」に替える。
  弱い指標のままだと成果を誤って判定する

### 3-2. キーワード詰め込み → 内容を正確に説明する書き方（3行）

対象: seo-strategy.md の 79 / 184 / 186

確定方針: キーワード詰め込みは効果なし〜逆効果。
「キーワードを入れるため」ではなく「内容を正確に説明するため」に書く、と優先順位を入れ替える。

**指示:** 上記の考え方で書き換える。画像の代替テキストとファイル名の項目が該当する。

### 3-3. 構造化データに注記を足す（4行）

対象: seo-strategy.md の 78 / 84、seo-concrete.md の 62、seo-measure.md の 47

確定方針: レビュー・評価の構造化データは入れない（二重管理を生む。自社レビューは対象外）。
ただし商品情報そのものは実装済みで問題ない。実測でレビュー系の記述は資料に0件。

**指示:** 削除しない。次の2点を注記として添える。
- レビュー・評価にあたるプロパティは入れない
- 金額は資料に書かず、料金表を唯一の管理元として参照する

### 3-4. 事例の「作成」→ 実在・許諾済みのみ（4行）

対象: seo-strategy.md の 199 / 208 / 209、seo-measure.md の 80

確定方針: 架空の事例は作らない。実在するものだけを載せる。掲載には許諾が必要。
「成功事例の作成」という書き方は、架空事例の製造を指示しているに等しい。

**指示:** 4行すべてに「実在・許諾済みのものに限る」ことを条件として明記し、
許諾手順の資料（`docs/customer-stories/` 配下）への参照を添える。

### 3-5. 「活用事例」→「活用シーン」（7行）

対象: seo-strategy.md の 52 / 54 / 113 / 141 / 222 / 296、seo-measure.md の 29

確定方針: 架空事例を全削除した結果、サイトに実在する事例は1件のみ。
業種・利用場面・機能の説明は事実として言えるため、「業種別の活用シーン」として再構成済み。

**指示:** 「事例」を「活用シーン」に統一する。「業種別・シーン別」という枠組み自体は現行方針と一致するので変えない。

### 3-6. 完了済みの施策に「実施済み」の注記を足す（20行）

対象:
- seo-strategy.md の 61 / 62 / 63 / 64 / 108 / 110 / 114 / 115
- seo-concrete.md の 9 / 10 / 16 / 19 / 20 / 21 / 30 / 48 / 146 / 152
- seo-measure.md の 35 / 49

**指示:** 削除しない。各項目に「実施済み」である旨と、どのフェーズで実施したかの1行を添える。
削除すると、なぜ現在の形になっているかの経緯が失われ、将来もとの方式に逆戻りする余地を残す。

該当する完了済み施策の代表例:
- サイトマップは外部パッケージを使わず標準機能で実装済み。旧方式のファイルは削除済み
- 全ページが「自分はトップページの複製である」と申告していた不具合は修正済み
- パンくずの構造化データは共通部品で出力済み
- よくある質問の構造化データは8問で実装済み
- クローラー向け指示ファイルは新方式で作成済み。サイトマップの宣言も追加済み
- ブログのURL階層は実装済み

### 3-7. AMP の記述を削除（4行）

対象: seo-concrete.md の 136 / 140 / 141 / 231

確定方針: AMP は Google が事実上終了させた施策。不要と判定済み。

**指示:** 4行とも削除する。231行目は実施スケジュール表の項目なので行ごと落とす。

### 3-8. フォルダの場所の誤り（6行）

対象: seo-concrete.md の 6 / 40 / 96 / 125 / 170 / 184

実測: 資料は `app/` 配下と書いているが、実際は `src/app/` 配下。
同じ資料の27行目・200行目では正しく `src/` と書けており、表記が不統一。

**指示:** 実在する場所に統一する。
実在しない場所を指したまま作業指示に使うと、新しいフォルダを作って二重になる事故が起きる。

---

## 第4部【危険】実行すると壊れる指示（seo-concrete.md:146〜154, 174）

### 4-1. クローラー向け指示ファイルの置き場所が誤っている

148行目が指す場所は、現在の実装場所と違う。実測で、資料が指す場所にファイルは存在せず、
別方式で実装されている（コミット e0450e1 で作成済み）。

<risk level="high">
資料の指示どおりに古い場所へファイルを置くと、現在動いている実装が使われなくなる恐れがある。
[推測] 同種の事象として、画像ファイルで「古い置き場所が転送設定より優先される」実測がフェーズ1にある。
クローラー向けファイルでも同じことが起きるかは未検証だが、
**現在動いている実装と食い違う指示を残すこと自体が危険**である。
</risk>

**指示:**
- 148行目の置き場所を、現在の実装場所に直す
- あわせて「古い置き場所にファイルを新規作成しないこと」を注記する

### 4-2. クロール制御の記述に歯止めがない（3行）

対象: seo-concrete.md の 151 / 153 / 174

背景: 以前、外部サービスが自動生成した設定によって主要AIクローラー9種が全ブロックされ、
AIから403で弾かれる状態になっていた。現在は解除済みで、全体を許可する設定になっている。

**指示:** 施策自体は一般には妥当なので削除しない。次の歯止めを1行足す。
- 拒否設定を増やす方向の変更はしない
- AIクローラーを対象にしない

### 4-3. 外部サービスの設定について資料に記述が一切ない（0件）

実測: 3資料に該当する外部サービス名は0件。

**指示:** クローラー関連の節（seo-concrete.md 5章）の冒頭に、
引き継ぎノート フェーズ5の該当セクションにある2つの設定と「変更禁止」を転記する。
クローラー向けファイルを触る人が最初に読む場所に置かないと意味がない。

---

## 第5部 個別の修正（10行）

| 行 | 現状の問題 | 指示 |
|---|---|---|
| strategy:6 | 「その場ですぐに人脈構築ができる」— 自分側の成果の断定。削除対象の表現基準に該当 | 前段（複数SNS集約・QR共有）は実在するので残し、後段のみ差し替える |
| strategy:93 | 金額「500」— **実測で誤り。実際は月額550円** | 金額を書かない。「金額は料金表を唯一の管理元とする」旨に置き換える。資料に金額を書くと管理箇所が5箇所目になる |
| strategy:124 | 「5つのビジネス効率化」— 「効率化を実現する」は削除対象として名指しされた表現そのもの | 記事タイトル案から効率化の断定を外す |
| strategy:148 | 「CRM連携による営業効率化」— **実測でサイトに0件。連携機能は提供していない** | 記事テーマから外す。書くと存在しない連携機能があると誤解される |
| strategy:226 | 「7日間無料」— **実測で正しい**（料金表・無料体験ページ・本番HTMLに実在） | 修正不要。そのまま残す |
| strategy:227 | 「無料プランと有料プランの比較表」— **実測で恒久的な無料プランは存在しない**。¥0 の枠は「7日間無料体験」で期間限定 | 「無料体験と有料プランの比較」に直す。用語を「無料体験」に統一する |
| strategy:296 | 第3部 3-5 のとおり | 「活用シーン」に直す |
| strategy:301 | 「先駆者だけが使う」— 利用者層についての事実主張で裏付けがない | 削除する。印象・イメージの表現（第一印象を変革します 等）とは別類型 |
| strategy:338 | 「見せびらかし効果により自然な口コミやシェアが生まれる」— 測れる結果の断定 | 断定をやめる |
| concrete:180,190 | 「アナリティクス」— 自社サイトの解析ツール導入の話で、提供機能の説明ではないため矛盾ではない。ただし削除済みの機能名と同じ語 | 「サイト解析ツール」に言い換える。資料を読む人が機能として書き戻す誘因を消す |

---

## 第6部 修正しないもの（判断済み・手を付けないこと）

以下は検討したうえで「修正しない」と判断した。触らないこと。

| 対象 | 判断理由 |
|---|---|
| strategy:9「営業職、マーケティング職など対人接触の多い職種」 | ターゲット層の説明であり、入力項目の話ではない。語が同じだけ |
| strategy:146「デザインテンプレート集」 | 記事テーマ案であって自社機能の断定ではない |
| strategy:288-290 の競合比較表 | 明確に競合の機能として書かれており、自社機能の断定ではない |
| strategy:273「キーワードが自然に含まれているか」 | 「自然に」があるため詰め込み方針と衝突しない |
| measure:56「OGP完全実装」 | 未対応課題として引き継ぎノートに継続記録されており整合している |
| concrete:86 の画像形式変換 | **実測で完了済み**（画像36件中36件が変換済み）。3-6 と同じく実施済みの注記のみ足す |
| seo-concrete.md 全体の書き直し | **書き直さない。** 実装の指示書としての役目は終わっており、書き直すと引き継ぎノートの未対応事項と二重管理になる。冒頭の注意書きと第4部の打ち消しで足りる |

---

## 検算

| 部 | 行数 |
|---|---|
| 第1部 | 1 |
| 第2部 | 7（うち112行目は第1部と重複のため計上しない → 6） |
| 第3部 | 9 + 3 + 4 + 4 + 7 + 20 + 4 + 6 = 57 |
| 第4部 | 4 |
| 第5部 | 10（うち strategy:226 は修正不要、strategy:296 は 3-5 と重複） |
| 合計 | 72行（重複を除く） |
