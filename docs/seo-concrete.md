# テクニカルSEO基盤整備の具体的な実施内容

> ⚠️ **この資料は 2025年5月時点のものです。**
> その後の AI 検索最適化（LLMO）フェーズ0〜5 で方針が変更された箇所があり、
> 2026-07-27 に矛盾箇所を修正しました。
>
> **最新の確定方針は `docs/handover/` の引き継ぎノート（フェーズ0〜5）を参照してください。**
> 特に以下は本資料より優先します。
> - 「やらないこと（証拠が否定的な施策）」の一覧
> - サービスとして提供している機能・していない機能の確定リスト
> - Cloudflare とクローラー向け設定の変更禁止事項
>
> 本資料に書かれた施策を実施する前に、引き継ぎノートで否定されていないか確認すること。

テクニカルSEO基盤整備として、以下の具体的な施策を実施することをお勧めします。ディレクトリ構造を元に、それぞれの施策とその実装場所について説明します。
1. サイト構造の最適化
URLパス構造の見直し

対象ファイル: src/app/ディレクトリ配下のフォルダ構造
実装内容:

現在のdigital-namecardなどのパス構造を継続しつつ、SEO上重要な新規コンテンツは /digital-namecard/[キーワード] 形式で統一
→ **実施済み**（LLMOフェーズ開始前に実装。フェーズ1のサイトマップ修正時に実在URLとして確認）
ブログ記事のURL構造を /blog/digital-namecard/[記事タイトル] のように階層構造を明確化
→ **実施済み**（フェーズ1のサイトマップ修正時に `/blog/digital-namecard/what-is` 等を実在URLとして確認）



サイトマップの作成と最適化

対象ファイル: /src/app/sitemap.ts（Next.js の Metadata File 方式）
→ **実施済み**（フェーズ1: `public/sitemap.xml` は削除済み。旧ファイルを作り直さないこと）
実装内容:

現在のsitemap.xmlを最新コンテンツを含めて更新
→ **実施済み**（フェーズ1: 掲載URL22件へ更新。404だった2件を差し替え、末尾スラッシュによる308転送を解消、未登録の実在ページ4件を追加）
動的サイトマップ生成の仕組みを導入（next-sitemapなどのパッケージ利用）
→ **実施済み。ただし方式が異なる**（フェーズ1: Next.js標準機能で実装。**外部パッケージは不採用**。next-sitemap を導入し直さないこと）
画像サイトマップも追加して画像検索からの流入も促進
→ **実施済み**（フェーズ1: 画像情報9件を `src/app/sitemap.ts` に引き継ぎ済み）



パンくずリストの最適化

対象ファイル: /src/components/ui/Breadcrumb.tsx
実装内容:

構造化データ（BreadcrumbList）を完全実装
→ **実施済み**（フェーズ1で実装済みと確認。共通部品 `src/components/ui/Breadcrumb.tsx` が複数ページに出力している）
一貫性のあるパンくず表示の統一化



2. メタデータの最適化
ページごとのメタ情報最適化

対象ファイル:

/src/app/layout.tsx（サイト全体のデフォルトメタデータ）
各ページのpage.tsx内のmetadataオブジェクト


実施内容:

タイトルタグにキーワードを前方配置（例: デジタル名刺とは？| 複数SNSをまとめて共有 | Share）
メタディスクリプションの最適化（クリック率向上のためのベネフィット訴求）
カノニカルURLの設定（重複コンテンツ防止）
→ **実施済み**（フェーズ1: 全ページが「自分はトップページの複製である」と申告していた不具合を修正。`src/app/layout.tsx` の `alternates.canonical` を `"./"` に変更）



構造化データの実装強化

対象ファイル:

各ページのpage.tsx
または共通コンポーネントとして実装


実装内容:

Article、FAQPage、HowTo、Productなどのスキーマ対応
※ レビュー・評価にあたるプロパティ（aggregateRating / review）は入れない。二重管理を生み、自社レビューは Google のポリシー上も対象外
※ 金額は本資料に書かず、料金表を唯一の管理元として参照する
JSON-LDフォーマットでの実装



typescript// 実装例（JSON-LDスキーマ）
const structuredData = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": faqData.map((item) => ({
    "@type": "Question",
    "name": item.question,
    "acceptedAnswer": {
      "@type": "Answer",
      "text": item.answer
    }
  }))
};
3. パフォーマンス最適化
画像最適化

対象ファイル: /public/images/ 配下の画像ファイル
実装内容:

すべての画像のWebP形式への変換完了（一部すでに対応済み）
→ **一部実施済み**（実測: `/public/images/` 配下のPNG26件中24件にWebP版あり。OGP画像2件（`icons/ogp.png` / `icons/ogp_line.png`）はPNGのみ）
next/imageコンポーネントの適切な実装
画像の遅延読み込み（Lazy Loading）設定



JavaScript/CSSの最適化

対象ファイル:

/src/app/globals.css
/src/components/配下のコンポーネント


実装内容:

CSSの最適化（未使用スタイルの削除）
JavaScriptのコード分割
重要なCSSの抽出とインライン化



Core Web Vitals対応

対象ファイル: サイト全体
実装内容:

Largest Contentful Paint(LCP)の最適化（ヒーローイメージの最適化）
First Input Delay(FID)の改善（JavaScriptバンドルの最適化）
Cumulative Layout Shift(CLS)の低減（メディア要素のサイズ指定）



4. モバイル対応の強化
レスポンシブデザインの最適化

対象ファイル:

/src/components/ui/配下のUIコンポーネント
/src/app/globals.css


実装内容:

モバイルでのタップターゲットサイズの最適化（最小48px）
ビューポート設定の確認と修正
フォントサイズの調整（モバイルでの可読性向上）



5. インデックス最適化

> **【変更禁止】Cloudflare の設定（フェーズ0で確定。以後変更しないこと）**
>
> | 設定項目 | 値 |
> |---|---|
> | AIトレーニングボットをブロックする | ブロックしない（クローラーを許可する） |
> | robots.txt を管理する | robots.txt 設定を無効にする |
>
> この2設定を戻すと robots.txt が Cloudflare 生成に戻り、AIが再び403で弾かれる。
> 本章の作業に着手する前に必ず確認すること。

robots.txtの最適化
→ **実施済み**（フェーズ0: `src/app/robots.ts` を作成。全クローラーを許可し `/api/` のみ除外）

対象ファイル: /src/app/robots.ts（Next.js の Metadata File 方式で robots.txt を生成）
※ `/public/robots.txt` は現在の実装場所ではない。**古い置き場所にファイルを新規作成しないこと。**現在動いている `src/app/robots.ts` の出力が使われなくなる恐れがある
実装内容:

クロール指示の最適化
※ 拒否設定（Disallow）を増やす方向の変更はしない。AIクローラーを対象にしない
サイトマップへのリンク追加
→ **実施済み**（フェーズ0: `Sitemap: https://sns-share.com/sitemap.xml` を宣言済み）
ディレクトリ単位のクロール制御
※ 拒否設定（Disallow）を増やす方向の変更はしない。AIクローラーを対象にしない



ページスピード最適化

対象ファイル: 全ページ
実装内容:

リソースの圧縮（Gzip/Brotli）
HTTPキャッシュの最適化
Webフォントの最適化



クローラビリティの確保

対象ファイル: /src/appおよび/src/components配下
実装内容:

JavaScriptに依存しないフォールバック対応
クロール予算の最適化（重要ページへの優先クロール指示）
※ 拒否設定（Disallow）を増やす方向の変更はしない。AIクローラーを対象にしない
内部リンク構造の最適化



6. トラッキングの実装
サイト解析ツール導入・強化

対象ファイル:

/src/app/layout.tsx（グローバルスクリプト設定）
または専用コンポーネント作成


実装内容:

Google Analyticsの最適設定（GA4）
イベントトラッキングの実装（コンバージョンポイント）
Search Console連携の確認



コンバージョン計測の最適化

対象ファイル:

/src/components/sections/CTA.tsx
その他コンバージョンポイントのコンポーネント


実装内容:

イベント計測の実装
ゴール設定の最適化
コンバージョンファネルの設計



実装優先度と工数目安

最優先（1〜2週間）:

メタデータの最適化
構造化データの実装
画像最適化


優先度高（2〜4週間）:

サイト構造の最適化
パフォーマンス改善
トラッキング実装


中期対応（1〜2ヶ月）:

モバイル対応の強化



これらの施策を段階的に実施することで、サイト全体のSEOパフォーマンスを向上させ、検索エンジンからの流入増加とコンバージョン率の改善が期待できます。