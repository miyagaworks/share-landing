# 2026-07-26 LLMO（AI検索最適化）フェーズ1 引き継ぎノート

<reliability>
本ノートの `<known-fact>` は全て当セッション中に Super が curl / git / Read で直接実測した結果。
CC（実装担当）の報告をそのまま転記した箇所はない。全て Super 自身が再計測して裏を取っている。
`<unconfirmed>` は未検証の推論。ctx 残量は十分な状態で作成。

**前セッション（2026-07-25_LLMO-phase0-handover.md）には誤りが2件ある。**
§B「前ノートの訂正」を必ず先に読むこと。
</reliability>

---

<context>
前セッションでフェーズ0（AIクローラーの遮断解除）が完了。当セッションはフェーズ1として
D-1（サイトマップ修正）から着手し、その過程で新たに発見した問題を含めて4件を本番反映した。
参照資料は ~/Desktop/LLMO/ 配下3点（前ノート §G 参照）。
</context>

---

## A. 当セッションで完了した作業（全て本番反映済み）

<status>完了</status>

<known-fact>
### コミット一覧（全て main 直・push 済み・リモート反映を git ls-remote で実証）

| ハッシュ | 内容 |
|---|---|
| 676ff88 | サイトマップを Next.js 標準の自動生成方式へ移行 |
| fadd3a1 | 前セッションの引き継ぎノートを追加 |
| bc3d65d | 全ページの正規URL指定がトップを指す問題を修正 |
| 41eaa21 | トップページとFAQに構造化データを追加 |
| a939b33 | 会社概要ページのメタ情報の社名を正しい法人名に修正 |

### A-1. サイトマップの修正（D-1）

`public/sitemap.xml` を削除し `src/app/sitemap.ts` へ移行。外部パッケージは不採用
（Next.js 15.2.6 の標準機能で足りるため）。

本番実測（デプロイ後）:
```
掲載URL 22件 → 全件 HTTP 200（308転送・404ともに 0件）
末尾スラッシュ 0件
画像情報 9件（旧サイトマップから引き継ぎ）
```

修正内容:
- 404だった2件を差し替え（/blog/qr-code-namecard-guide → /blog/digital-namecard/qr-code-guide、
  /blog/sns-integration-business-card → /blog/digital-namecard/sns-integration）
- 末尾スラッシュ付き17件による308転送を解消
- 更新日を git log の実測値に修正（全ページ2025年。偽装せず事実を記載した）
- 未登録だった実在ページ4件を追加（/company/about、/company/service、
  /legal/transactions、/support/help）
- 除外2件: /blog/category/[slug]（generateStaticParams 未実装で静的列挙不可）、
  /blog/digital-namecard（記事リストが未実装の空ページ）

### A-2. 正規URL指定の修正（当セッションで新規発見）

`src/app/layout.tsx` の `alternates.canonical` が固定値 `"https://sns-share.com"` だったため、
**全22ページが「自分はトップページの複製である」と申告していた。**
サイトマップ修正の効果を打ち消す状態だった。

修正: 固定値を `"./"` に変更（1ファイル1行）。

根拠（node_modules の実装で確認済み。推測ではない）:
```
node_modules/next/dist/lib/metadata/resolvers/resolve-url.js
  resolveRelativeUrl:  "./" 始まりを path.posix.resolve(pathname, url) で解決
  resolveAbsoluteUrlWithPathname:  pathname === '/' のとき origin を返す
```
これにより、metadata をエクスポートできないクライアントコンポーネントのページ7件も
共通設定を継承して同時に解消された。

検証: ビルド成果物23ページ全てで canonical が自ページを指すことを Super 自身が確認（不一致0件）。
本番でも5ページを抜き取り実測し、全て正しい値を確認。

### A-3. 構造化データの追加（D-2）

| ページ | 追加した内容 |
|---|---|
| /support/faq | 質問と回答 8問（FAQPage / Question / Answer） |
| / | 運営会社（Organization）、サービス情報（SoftwareApplication）、料金9件（Offer） |

本番実測で反映を確認済み。値は全て画面表示から取得しており、作文・推測は含まない。

### A-4. 会社名の誤記修正（当セッションで新規発見）

`src/app/company/about/page.tsx` の metadata 4箇所が「株式会社Share」となっていた。

正しい社名の判定根拠（実測）:
| 場所 | 表記 |
|---|---|
| 会社概要ページの見出し・会社概要表 | 株式会社**Senrigan** |
| 利用規約 | 株式会社**Senrigan** |
| 特定商取引法に基づく表記 | 株式会社**Senrigan** |

法的文書が全て Senrigan であるため metadata 側が誤りと判定し、4箇所を修正。
画面表示・本文には一切触れていない。サービス名としての「Share（シェア）」は保持。
</known-fact>

---

## B. 前セッションのノートの訂正【重要】

<known-fact>
`docs/handover/2026-07-25_LLMO-phase0-handover.md` の以下2件は **Super の実測ミスによる誤り**。
そのまま信じると不要な作業をすることになる。

### B-1. D-3「トップページに h1 が存在しない」→ 誤り

実際には存在する。
```
実測: <h1>先進的なデジタル名刺でビジネスに差をつける</h1>
実装: src/components/sections/Hero.tsx:21
```
**D-3 は作業不要。タスクから削除した。**

誤った原因: h1 の中身が `<span class="block">先進的なデジタル名刺で</span>` と
`<span class="block mt-2">ビジネスに差をつける</span>` の2要素に分割されているため、
`<h1[^>]*>[^<]` 形式の検索でマッチしなかった。タグ直後にテキストがある前提で検索していた。

### B-2. D-2「構造化データは /digital-namecard に1件のみ」→ 誤り

実際には共通部品 `src/components/ui/Breadcrumb.tsx` がパンくずの構造化データを出力しており、
複数ページに存在していた。さらに /blog/digital-namecard/what-is には FAQPage も実装済みだった。

実測した当セッション着手時点の状況:
| ページ | 構造化データ |
|---|---|
| / | 0件 ← 本当に不足していたのはここ |
| /support/faq | 0件 ← 質問8問あるのに未実装だった |
| /digital-namecard、/free-trial、/case-studies | パンくずのみ |
| /blog/digital-namecard/what-is | パンくず + FAQPage |

誤った原因: 一部のページしか調べずに「全体で1件」と結論づけた。
</known-fact>

---

## C. 次セッションで着手すべきタスク

<next-action>
### C-1【最優先】D-4 コンテンツに統計・出典・引用を追加

Princeton GEO論文（KDD 2024）で最も効果が大きいとされた施策（可視性30〜40%向上）。
海外調査レポート（~/Desktop/LLMO/2026-07-23_海外LLMO調査レポート.md）に根拠あり。

当セッションで実測した現状:
```
外部への出典リンク数
  /digital-namecard              0件
  /digital-namecard/about        0件
  /blog/digital-namecard/what-is 0件
  /case-studies                  0件
```

<unconfirmed>
/case-studies の本文に「45%」「35%」「150%」等の数値表現が存在することを確認したが、
**その数値に出典があるか、自社実績なのか、架空の想定例なのかは未確認。**
調査コマンドがタイムアウトして完了できなかった。次セッションで最初に確認すること。

出典のない統計値を掲載している場合、AI検索対策以前に表示の正確性の問題になる。
実データに基づかない数値であれば、出典追加ではなく数値の見直しが必要になる。
着手前に必ずユーザーに事実関係を確認すること。
</unconfirmed>

進め方の案:
1. /case-studies の数値の出所をユーザーに確認する（最優先）
2. 業界統計・公的データ（総務省の通信利用動向調査等）への出典リンクを本文に追加する
3. 自社の一次データ（登録者数・共有回数・業種別利用率）が公開可能かユーザーに確認する
   → 公開できれば、他社が引用できない独自素材になり最も効果が高い

### C-2 D-5 デジタルPR / ブランド言及の獲得

Ahrefs 7.5万ブランド調査で最強の相関（0.664）。バックリンク（0.218）より圧倒的。
実行に時間がかかるため企画を早めに開始する。
</next-action>

---

## D. 当セッションで新たに判明した課題（未対応）

<known-fact>
### D-1. 料金情報が2箇所に重複している【優先度：中】
`src/components/sections/Pricing.tsx`（表示用）と `src/app/page.tsx`（構造化データ用）に
価格が別々にハードコードされている。料金改定時に片方を更新し忘れると、
画面表示と機械向け情報が食い違う。現状は両ファイルに警告コメントで対処。
恒久対策はプラン定義を共有モジュールへ切り出すことだが、Pricing.tsx の改変が必要。

### D-2. SNS共有用のアドレス（og:url）が固定値【優先度：低】
layout.tsx の `openGraph.url` が `https://sns-share.com` 固定。canonical と同じ問題だが、
影響はSNS共有時の表示に限られる。`"./"` で同様に解決できる見込み。
ただし openGraph はページ側で定義すると親を丸ごと置換する仕様のため、
openGraph を定義している page.tsx 15件の扱いを個別に確認する必要がある。

### D-3. /blog/digital-namecard が空ページのまま公開されている【優先度：中】
h1 と説明文はあるが記事リストが未実装（`{/* ここに記事リストを表示 */}` のみ）。
サイトマップからは除外したが、ページ自体は200で配信され続けている。
実装するか非公開にするかの判断が必要。

### D-4. next.config.ts の /logo.svg の転送設定は実質無効
public/logo.svg（4,597バイト）が存在し、Next.js は public を転送設定より優先するため、
転送設定は使われていない。本番・手元・アプリ側の3者が同一サイズであることを実測確認済み。
害はないが、無用な設定として整理の余地がある。
</known-fact>

---

## E. Cloudflare の設定（フェーズ0から継続・変更禁止）

<known-fact>
当セッション着手時に実測し、前セッションの設定が維持されていることを確認済み。

| 設定項目 | 値 |
|---|---|
| AIトレーニングボットをブロックする | ブロックしない（クローラーを許可する） |
| robots.txt を管理する | robots.txt 設定を無効にする |

実測結果:
```
https://sns-share.com/robots.txt        → 200（Cloudflare生成の痕跡 0件）
GPTBot でトップ・/digital-namecard      → 200
ClaudeBot でトップ                      → 200
```
この2設定を戻すと robots.txt がCloudflare生成に戻り、AIが再び403で弾かれる。
</known-fact>

---

## F. 既存SEO資料との整合（当セッションで確認）

<known-fact>
docs/ 配下の3資料（seo-strategy.md / seo-concrete.md / seo-measure.md）を読了。

**一致**: サイトマップ整備・構造化データ実装・独自調査データの公開は、
LLMO施策（D-1・D-2・D-4）とそのまま重なる。方向性の衝突はない。

**古くなっている記述（seo-concrete.md）**:
- クローラー制御ファイルを「public配下」と記載 → 現在は src/app/robots.ts 方式
- フォルダ構成を `app/` と記載 → 実際は `src/app/`
- 「AMPの導入検討」→ Google が事実上終了させた施策。不要
- 「動的サイトマップは next-sitemap 等のパッケージ利用」→ 標準機能で実現済み

**注意が必要な記述（seo-strategy.md）**:
「画像のalt属性・ファイル名にキーワードを含める」とあるが、海外調査では
キーワード詰め込みは効果なし〜逆効果とされる。「キーワードを入れるため」ではなく
「内容を正確に説明するため」に書く、という優先順位の入れ替えが必要。

3資料の更新は未実施。D-4 完了後にまとめて行うのが効率的。
</known-fact>

---

## G. 当セッションの反省点

<reflection>
### G-1【重大】前セッションで対象要素を特定せずに検索し、2件の誤った結論を出した
§B の2件がこれにあたる。h1 の中身が入れ子タグだったこと、構造化データが共通部品で
出力されていたことを見落とした。**画面の状態を判定するときは、対象要素を特定して
抽出すること。単純なパターン検索で「無い」と結論づけないこと。**

### G-2 サイトマップの件数を誤って指示した
Super が実ページ23件から「22件になるはず」と計算を誤って指示（正しくは23件）。
CCが空ページ1件を追加除外した結果たまたま22件になったが、
件数合わせのための除外だった可能性が残る。除外理由自体は中身を確認して妥当と判定した。
**期待値を指示するときは、その数字の導出過程を自分で検算すること。**

### G-3 CCの誤った報告を実測で覆せた（良かった点）
CCが「画像情報は Next.js の型が対応していないため引き継げない」と報告したが、
型定義（`images?: string[]`）を確認して誤りと判定し、9件の情報損失を防いだ。
CCの報告は「意図」であって「事実」ではない、という規律が機能した。

### G-4 curl の出力肥大は再発しなかった
前セッションの反省を受け、全ての実測で `-o /dev/null` / `grep -c` / 件数のみ出力を徹底した。
ただし複雑な正規表現による調査が1回タイムアウトした（§C-1 の未確認事項）。
</reflection>

---

## H. やらないこと（証拠が否定的・調査レポート準拠）

<known-fact>
| 施策 | 判定根拠 |
|---|---|
| llms.txt の設置 | Ahrefs 13.7万ドメイン実測で97%が未アクセス。Google公式も不使用を明言 |
| キーワード詰め込み | Princeton論文で効果なし〜逆効果 |
| バックリンクの量的獲得 | 相関0.10〜0.218と弱い。リンクよりメンション |
| 広告費でAI可視性を買う | 相関0.215と弱い |
| トップページへの h1 追加 | **既に存在する**（§B-1） |
</known-fact>
